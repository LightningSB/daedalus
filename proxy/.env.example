# Bun SSH API
PORT=8080

# Optional: override the proxy's own Docker container ID.
# Auto-discovery reads /proc/self/mountinfo and the HOSTNAME env var, which
# works in most Docker deployments.  Set this when it fails (cgroup v2,
# nested containers, or when the Docker socket is not mounted).
# Find the value with:  docker ps | grep daedalus-proxy
# SELF_CONTAINER_ID=

# MinIO
MINIO_ENDPOINT=minio.wheelbase.io
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=wheelbase-admin
MINIO_SECRET_KEY=change-me
MINIO_BUCKET=daedalus

# Comma-separated SSH host allowlist
SSH_ALLOWED_HOSTS=34.186.124.156

# Public app URL used in magic links
APP_ORIGIN=https://daedalus.wheelbase.io

# Telegram bot token used to send magic links via DM
TELEGRAM_BOT_TOKEN=123456:replace-me
